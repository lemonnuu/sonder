{
  "name": "rspress-plugin-devkit",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/rspress-plugin-devkit/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p tsconfig.json",
        "cwd": "packages/rspress-plugin-devkit"
      },
      "outputs": ["{projectRoot}/dist"]
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -w -p tsconfig.json",
        "cwd": "packages/rspress-plugin-devkit"
      }
    },
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "command": "rimraf dist",
        "cwd": "packages/rspress-plugin-devkit"
      }
    }
  },
  "tags": ["type:lib", "scope:plugin"]
}
